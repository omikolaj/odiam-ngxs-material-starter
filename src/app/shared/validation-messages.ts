/**
 * Constant for display message when input field is required.
 */
export const ValidationMessage_Required = 'This field is required.';

/**
 * Constant for display message when input field has incorrectly formatted email.
 */
export const ValidationMessage_Email = 'Invalid e-mail format.';

/**
 * If field input is invalid but the control has no errors associated with it.
 * This serves as the generic error message.
 */
export const ValidationMessage_Invalid = 'Input validation error.';
